const jsonAPI = 'https://jsonplaceholder.typicode.com';
const carsAPI = 'http://owu.linkpc.net/carsAPI/v1';

const urls = {
    users: '/users',
    comments: '/comments',
    cars: '/cars'
}

export {jsonAPI, carsAPI, urls}