import React from 'react';

import css from './Header.module.css'

const Header = () => {
    return (
        <div className={css.Header}>
            You're looking at the header. Even if it's not necessary, it's here :)
        </div>
    );
};

export default Header;